{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@types/katex": "^0.16.0",
    "katex": "^0.16.4",
    "nuxt": "3.0.0",
    "nuxt-runtime-compiler": "^1.1.3"
  }
}
